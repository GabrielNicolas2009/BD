create database biblioteca;
use biblioteca;
create table livros (
livro_id int primary key,
titulo varchar(100),
autor varchar(75),
ano_publucacao date
);
create table usuarios (
usuario_id int primary key ,
nome varchar(75),
email varchar(75)
);
create table emprestimos(
emprestimo_id int primary key,
livro_id int,
usuario_id int,
data_emprestimo date,
data_devolucao date,

foreign key (livro_id) references livros(livro_id),
foreign key (usuario_id) references usuarios(usuario_id)
);
create table generos(
genero_id int primary key,
nome varchar(50)
);
create table livro_genero(
livro_id int,
genero_id int,

foreign key (livro_id) references livros(livro_id),
foreign key (genero_id) references generos(genero_id)
);

insert into livros(livros_id, titulo, autor, ano_publicacao)
values (1, "Soul Eater", "Atsushi Okubo", "2004-06-22"),
	   (2, "Pokémon Black and White", "Hidenori Kusaka", "2010-09-3"),
       (3, "O Pequeno Príncipe", "Antoine de Saint-Exupéry", "1943-04-13"),
       (4, "Pinóquio: Wood Edition", "Carlo Collodi", "1883-02-28"),
       (5, "")
